import { gridConfig } from "../../scene/gen/procedural/grid/constants.js";

export function createBuilderSettings() {
    // gridConfig.cellSize = 300; //probably want to reload entire scene b/c landscape, highlight changes
}